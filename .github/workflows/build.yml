name: Java CI

on:
  push:
    branches:
      - main

  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions/checkout@v3

      - name: Build Project
        uses: ./.github/actions/build-project
        with:
          java-version: 18
